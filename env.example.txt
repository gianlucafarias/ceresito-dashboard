UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=

NEXTAUTH_URL = https://authpanel.ceres.gob.ar
NEXTAUTH_SECRET=
NEXT_PUBLIC_MAPS_API_KEYS=""

DATABASE_URL=""

RESPONSES_SHEET_API=
RESPONSES_SHEET_ID=''

GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=""
CHAT_PORT=

# APIs Externas
# API Principal de Ceres (reclamos, encuestas, contactos, conversaciones, etc.)
NEXT_PUBLIC_API_URL=

# API de Servicios Externos (profesionales, categorías, certificaciones, etc.)
NEXT_PUBLIC_SERVICES_API_URL=https://ceresenred.ceres.gob.ar
ADMIN_API_KEY=tu-api-key-aqui

# Configuración para Server Actions
# Configura los orígenes permitidos separados por comas
# NEXT_PUBLIC_ALLOWED_ORIGINS=
# Core API migration flags (same variable names in all environments)
# Set dev/prod values in the corresponding .env file loaded by NODE_ENV.
CORE_API_LEGACY_BASE_URL=https://api.ceres.gob.ar/api/api
CORE_API_V1_BASE_URL=https://api.ceres.gob.ar/api/v1
# Optional: if empty, server uses ADMIN_API_KEY
CORE_API_ADMIN_KEY=
# legacy | v1 (global toggle for current migration cycle)
CORE_API_TARGET=legacy
# true | false
CORE_API_FAILOVER_TO_LEGACY=true
