UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=

NEXTAUTH_URL = https://authpanel.ceres.gob.ar
NEXTAUTH_SECRET=
NEXT_PUBLIC_MAPS_API_KEYS=""

DATABASE_URL=""

RESPONSES_SHEET_API=
RESPONSES_SHEET_ID=''

GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=""
CHAT_PORT=

# APIs Externas
# API Principal de Ceres (reclamos, encuestas, contactos, conversaciones, etc.)
NEXT_PUBLIC_API_URL=

# API de Servicios Externos (profesionales, categorías, certificaciones, etc.)
NEXT_PUBLIC_SERVICES_API_URL=https://ceresenred.ceres.gob.ar
ADMIN_API_KEY=tu-api-key-aqui

# Configuración para Server Actions
# Configura los orígenes permitidos separados por comas
# NEXT_PUBLIC_ALLOWED_ORIGINS=
# Core API migration flags (legacy /api/api -> new /api/v1)
# Generic fallback (used if env-specific variables are not defined)
CORE_API_LEGACY_BASE_URL=https://api.ceres.gob.ar/api/api
CORE_API_V1_BASE_URL=https://api.ceres.gob.ar/api/v1
# Optional: if empty, server will use ADMIN_API_KEY
CORE_API_ADMIN_KEY=
# legacy | v1
CORE_API_TARGET_USERS_COUNT=legacy
# true | false
CORE_API_FAILOVER_TO_LEGACY=true

# Environment-specific overrides (automatic by NODE_ENV)
# NODE_ENV=development -> *_DEV (or *_DEVELOPMENT / *_LOCAL)
CORE_API_LEGACY_BASE_URL_DEV=http://localhost:3004/api/api
CORE_API_V1_BASE_URL_DEV=http://localhost:3022/api/v1
CORE_API_ADMIN_KEY_DEV=
CORE_API_TARGET_USERS_COUNT_DEV=legacy
CORE_API_FAILOVER_TO_LEGACY_DEV=true

# NODE_ENV=production -> *_PROD (or *_PRODUCTION)
CORE_API_LEGACY_BASE_URL_PROD=https://api.ceres.gob.ar/api/api
CORE_API_V1_BASE_URL_PROD=https://api.ceres.gob.ar/api/v1
CORE_API_ADMIN_KEY_PROD=
CORE_API_TARGET_USERS_COUNT_PROD=legacy
CORE_API_FAILOVER_TO_LEGACY_PROD=true
